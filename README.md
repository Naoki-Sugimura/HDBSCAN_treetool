# HDBSCAN_treetool
クラスタリングに自作HDBSCANを用いた際のプログラム
## 🌳 仮想環境のセットアップ (Conda)

このプロジェクトを実行するには、`environment.yml`ファイルを使用して、すべての必要な依存関係を含む仮想環境を作成することを強く推奨します。

### 1. 仮想環境の作成

プロジェクトのルートディレクトリで以下のコマンドを実行し、`treetool`という名前の仮想環境を作成します。

```bash
conda env create --file environment.yml
